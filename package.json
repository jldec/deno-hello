{
  "name": "scanode",
  "version": "2.0.0",
  "description": "ESM module - crawls a website, validating that all the links on the site which point to the same orgin can be fetched.",
  "main": "scanurl.mjs",
  "bin": {
    "scanode": "./bin/scanode.mjs"
  },
  "dependencies": {
    "node-fetch": "^2.6.1",
    "parse5": "^6.0.1",
    "pub-server": "^2.8.0"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/jldec/deno-hello.git"
  },
  "scripts": {
    "scan": "scanode",
    "pub": "pub",
    "preview": "pub -S out",
    "generate": "rm -r out ; pub -O -E"
  },
  "author": "Jürgen Leschner",
  "license": "MIT"
}
